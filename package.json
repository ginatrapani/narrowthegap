{
  "name": "narrowthegap",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16",
    "react-bootstrap-typeahead": "3.4.7",
    "react-dom": "^16",
    "react-helmet": "^5.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "^3.4.1"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build && react-snap",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "husky": "^4.2.5",
    "prettier": "2.0.4",
    "pretty-quick": "^2.0.1",
    "react-snap": "^1.23.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
